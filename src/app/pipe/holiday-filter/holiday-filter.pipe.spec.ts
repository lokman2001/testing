import { HolidayFilterPipe } from './holiday-filter.pipe';

describe('HolidayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HolidayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
